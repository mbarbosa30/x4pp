/**
 * Platform-wide constants
 */

/**
 * Default minimum bid for messages sent to unregistered wallet addresses
 * This is used when a message is sent to a wallet address that hasn't registered yet
 */
export const PLATFORM_DEFAULT_MIN_BID = 0.10;
